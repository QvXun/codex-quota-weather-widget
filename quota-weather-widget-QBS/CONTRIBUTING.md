# 参与修改

1. 不提交 .env、本地配置、窗口状态、日志、缓存、截图、构建输出或认证材料。
2. 不把真实位置、额度、重置时间、用户名、邮箱和绝对路径写入代码、文档或测试。
3. 新增网络请求前，必须在 README 和隐私说明中写明用途、发送字段和关闭方式。
4. 新增数据提供器必须默认安全，且不能读取认证文件或保存 Token。
5. 每次修改后运行 PowerShell 语法检查和 DEMO 自检；提交前复查暂存区。
6. 上游许可证未确认前，不要上传或公开分发本副本。
